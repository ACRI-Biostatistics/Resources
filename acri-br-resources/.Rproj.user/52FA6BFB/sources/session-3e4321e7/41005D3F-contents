
---
title: "Making a Summary Table of the Covariate Balance"
author: "Yutian T. Thompson"
date: Nov 2025
output:
  word_document
    
---

```{r, echo=F, message=F}
# cat("Working directory: ", getwd())
library(knitr)
library(flexdashboard)
opts_knit$set(root.dir='../../../')  #Don't combine this call with any other chunk -especially one that uses file paths.
```

```{r set-options, echo=F}
# cat("Working directory: ", getwd())
report_render_start_time <- Sys.time()
opts_chunk$set(
  results      = 'show',
  comment      = NA,
  tidy         = FALSE,
  # dpi        = 400,
  # out.width  = "650px", #This affects only the markdown, not the underlying png file.  The height will be scaled appropriately.
  fig.width    = 30,
  fig.height   = 30,
  fig.path     = 'figure-dashboard-png/'
)

echo_chunks    <- FALSE #Toggle for debugging.
message_chunks <- FALSE #Toggle for debugging.
options(width=100) #So the output is 25% wider than the default.
read_chunk("C:/Users/YTHOMPSO/Documents/GitHub/silovsky-ffpsa-1/analysis/personal/yutian/covariate-balance-check-table.R")
#This allows knitr to call chunks tagged in the underlying *.R file.
```




<!-- Load packages, or at least verify they're available on the local machine.  Suppress the output when loading packages. -->
```{r load-packages, echo=echo_chunks, results='hide', message=message_chunks, warning=FALSE}
```

<!-- Load any global functions and variables declared in the R file.  Suppress the output. -->
```{r declare-globals, echo=echo_chunks, results='hide', message=message_chunks, warning=FALSE}
```

<!-- Load the datasets.   -->
```{r load-data, echo=echo_chunks, results='hide', message=message_chunks , warning=FALSE}
```

<!-- Tweak the datasets.   -->
```{r covariate-balance, echo=echo_chunks, results='show', message=message_chunks, warning=FALSE }
```

